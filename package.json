{
  "name": "universe-sim-v1",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d docs"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "three": "^0.167.0",
    "vite": "^5.4.0"
  }
}
